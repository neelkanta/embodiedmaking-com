Language for Product Instances

An expressive typology of entities (around 30) and possible relationship between entities (around 15). Instances can create unique expressions based on these 30 keys and 15 fingers.