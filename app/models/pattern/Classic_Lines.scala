package models.pattern

import models.Pattern

object Classic_Lines extends Pattern(
  title = "Classic Lines",

  summary =
    """
      Any product stakeholder, irrespective of whether they are from marketing or technology, should be able
      to configure the most frequently occuring changes themselves.
    """
)
